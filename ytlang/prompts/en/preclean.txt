You are given transcript fragments from automatic speech recognition (ASR). Each fragment has a "seconds" timestamp and a "text" field.

Your task:
1. Restore punctuation (. ? !) and capitalization.
   A period is a sentence-ending mark ONLY when it ends a complete thought. Do NOT treat these as sentence-ending periods:
   - Titles and honorifics: Mr. Mrs. Ms. Dr. Prof. Sr. Jr.
   - Abbreviations: U.S. U.K. U.N. e.g. i.e. etc. vs. approx.
   - Decimals and version numbers: 1.25, 3.14, v2.0
   - Initials in names: J.K. Rowling, A.I.
2. Merge consecutive fragments that belong to the same sentence.
3. Each output entry must contain EXACTLY ONE sentence — never combine two sentences into one entry.
4. Use the timestamp of the FIRST fragment that contributes to each output sentence.
5. Keep wording faithful to the original speech — do NOT paraphrase or add content.
6. Remove only clearly accidental duplicate words (e.g. stuttered repetition).
7. When 'at' is followed by a word that looks like a username (e.g. 'at ninja', 'at drlupo'), write it as a Twitter/social-media handle: '@ninja', '@DrLupo'. Do NOT treat it as a location.
8. Keep non-speech markers like [Music] as their own entry with their original timestamp.

Return ONLY valid JSON — an array of objects with exactly these fields:
  "seconds" (number), "en" (string), "zh" (empty string ""), "note" (empty string "")

No explanation, no markdown, no code fences.